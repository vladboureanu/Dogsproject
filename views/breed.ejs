<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
  <div class="container">
    <h1 class="my-4"><%= title %></h1>
    <form action="<%= action %>" method="POST">
      <div class="form-group">
        <label for="breed">Breed:</label>
        <input type="text" class="form-control" id="breed" name="breed" value="<%= breed ? breed.breed : '' %>" required>
      </div>
      <div class="form-group">
        <label for="description">Description:</label>
        <textarea class="form-control" id="description" name="description" rows="5"><%= breed ? breed.description : '' %></textarea>
      </div>
      <div class="form-group">
        <label for="origin">Origin:</label>
        <input type="text" class="form-control" id="origin" name="origin" value="<%= breed ? breed.origin : '' %>" required>
      </div>
      <div class="form-group">
        <label for="life_span">Life Span:</label>
        <input type="text" class="form-control" id="life_span" name="life_span" value="<%= breed ? breed.life_span : '' %>" required>
      </div>
      <div class="form-group">
        <label for="temperament">Temperament:</label>
        <input type="text" class="form-control" id="temperament" name="temperament" value="<%= breed ? breed.temperament : '' %>" required>
      </div>
      <div class="form-group">
        <label for="image_url">Image URL:</label>
        <input type="text" class="form-control" id="image_url" name="image_url" value="<%= breed ? breed.image_url : '' %>" required>
      </div>
      <button type="submit" class="btn btn-primary">Submit</button>
    </form>
  </div>
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
